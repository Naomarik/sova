# pi-web — Design Notes

The design spec now lives in `spec/`, starting at [`spec/overview.md`](spec/overview.md).

`§N` references throughout the codebase map to `spec/<NN>-*.md` files (for example, §10 → `spec/10-insights.md`, §4b → `spec/04b-images.md`).
