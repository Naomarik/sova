# 01 · App shell

> Moved. This document now lives at [.sova/spec/claims/app/shell.md](../.sova/spec/claims/app/shell.md) as `§app/shell`.
> Its H1/H2 headings and the `§N` citations resolve through `.sova/spec/migration/legacy-map.json`.
> Deeper references (H3, bold lead-ins, table rows) resolve by searching the new file for the words,
> which it kept byte for byte. The committed original is `.sova/spec/migration/legacy/head/spec/01-app-shell.md`.
