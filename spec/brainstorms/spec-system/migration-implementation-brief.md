# Approved correction: documentation home plus isolated feature drafts

User clarified original intent: .sova/spec is BOTH project documentation and graph/freshness machinery. Existing Sova product spec/ must be ported, not permanently indexed. Other projects have zero prose; author baseline behavior piecemeal on feature work. Unimplemented/uncommitted proposed changes must not mutate authoritative current docs. User suggests copy spec, edit prose, align, then writer promotes; parent clarified alignment approves intent, implementation+verification precede promotion. User grants autonomous iteration and requests final fresh Opus high review.

## Fixed principles
- Foldaidev § identifiers, no required source annotations, any language/project, no required Git/incumbent docs.
- .sova/spec/claims + manifest current documentation. Migration transfers requirement authority, NOT proof of implementation; state migrated/unreviewed explicitly.
- Draft is COMPLETE literal spec copy (manifest plus ALL claim files) with immutable baseline; do not copy subset while retaining dangling complete manifest.
- Proposed/aligned != current. Explicit promotion of implemented verified subset only, no blind overwrite.
- Git project: require named existing implementation commit and per-promise verification/evidence, not committing unrelated changes. Machine verifies revision/object/input relationships, cannot prove semantic implementation.
- No-Git: exact retained implementation snapshot and recorded review evidence; do not make promotion impossible merely because Git absent.
- No mass new dependency fields or overlay engine; small file/record three-way comparisons. Status metadata one machine-readable home, prose proposal not duplicated mutable status.
- Preserve existing pilot/experimental evidence and claims historical meanings, research spec/brainstorms not product migration.
- Preserve ALL dirty user/other-session work. NEVER git reset/checkout/restore dirty files. Snapshot working bytes alongside HEAD; copy dirty delta into isolated draft BEFORE replacing a legacy doc with redirect. If file changed since captured, refuse replacing it and report/retry, don't overwrite.
- All new project artifacts inside .sova/spec; canonical distributable tools pi-config/extensions/spec/core; vendored copies updated only after validation.

## Implementation ownership
Core worker: sova-spec.mjs/core tests and core README only. Add --spec project-relative directory for draft graph, keep code paths rooted in project. Keep all .sova/spec excluded census not just chosen draft. Support normal H3+ non-declaring prose so docs migrate losslessly; only H1/H2 § headings declare, reject mistaken § declarations deeper. Add note kind for reference/decision docs and optional explicit authority/evidence status labels without interpreting migrated as verified. Preserve old format and tests, coordinate minimal output contract. Missing requires means unknown only behavioral records; status never hash proof.
Draft worker: new sova-spec-draft.mjs + draft tests + draft workflow README only. New full copy; status/diff; check via trusted core --spec; promotion preview by default explicit --write. Safe names/paths, no symlinks/secrets, never project script execution. Baseline hashes and retained files; old/base/new comparison; unrelated current file changes preserved. Per-record manifest merge with conflicts on same changed record; full graph validation of candidate merged result before writes. Prose files conservatively conflict if both sides differ (identical desired/current can no-op). Explicit deletions/new files, selected changes, no silent overwritten current. Atomic-or-rollback multi-file transaction/cooperating lock; interrupted/pending transaction refuses further writes until explicit recovery, documented limitations. No automatic semantic verification/adoption. Git implementation input evidence and no-Git snapshot tests. May start minimal diff/status then promotion; don't build full Git.
Migration worker: .sova/spec current claims/manifest and migration inventory + preserved legacy working/head bytes + drafts/legacy-working. Map all root product spec/*.md except brainstorms, preserve every prose/table/fence/rationale condition with reversible heading/link transforms. IDs namespace names no digits, deterministic collision handling, resolve every old §N via map. Pilot live IDs keep meaning, move historical candidate text to explicit candidate/legacy material without duplicate authority; cannot use same ID twice or cut/reuse semantics. Existing current docs from committed bytes; dirty worktree variants stored as draft alongside exact originals, not current. No product implementation edits. Prepare legacy redirect edits as /tmp patch/plan for parent to apply after final capture checks, not overwrite concurrent docs directly. Inventory includes hashes/status, content parity check inverse transforms, internal link/ID resolution and provenance accuracy. No authored derived resolution data. Draft feature untracked legacy doc preserved proposal only.
Docs integration worker: root project instructions/design skill and minor mode changes are staged as patch outside live tree, no worker edits servergraph. Other spec README/USAGE wait schema/mapping final then update. State new .sova/spec authority, legacy paths redirect, proposed docs isolated, reconcile known baseline separately, no existing spec prerequisite. Minimal prompt within reasonable token cost; actual commands only. Prepare docs link changes only own files as agreed team_msg; don't modify draft/core docs workers own.

## Validation
- Original content inventory vs migrated bytes through deterministic reversible transforms; no paragraph dropped, no requirement tightened/loosened by 'shortening'. Retain all exact originals and hashes.
- Pilot candidates not fabricated verification; migrated docs own requirement authority while implementation evidence unreviewed visible.
- Legacy paths/§N map readable with no source-comment retrofit, old brainstorms untouched.
- No-Git/no-spec fixture creates docs, new draft/alignment changes leave current byte-identical; proposed graph scope works; commit/snapshot gates block premature promotion; conflicts preserve current and draft; unrelated edits survive; selected promotion tests whole graph consistency; tampering/races/symlinks/malformed fields rejected.
- Existing177 spec tests retained or deliberately updated for nested prose support; project typecheck/build/tests and mode tests.
- Fresh Opus high review after first complete implementation: functionality+lossless migration+draft/current separation+portability+lightweightness. Fix material findings before final.
